platform: android

android:
  module: app
  build_variant: release
  package_name: com.example.myapp

workflows:
  beta:
    - action: lint
    - action: test
    - action: archive
    - action: play-store
      options:
        track: internal
